import { RegionType } from './regionType.entity';

export class Region {
    id: number;
    regionName: string;
    regionNameAr: string;
    regionCode: string;
    regionType: RegionType;
    regionPath: string;
}
